import { UpdatePasswordForm } from "@/components/auth/update-password-form";

export const metadata = { title: "Set a new password · Channel Cast" };

export default function UpdatePasswordPage() {
  return <UpdatePasswordForm />;
}
