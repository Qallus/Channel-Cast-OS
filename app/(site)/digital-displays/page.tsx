import { ScenePage, SCENE_META } from "@/components/site/scene-page";

export const metadata = SCENE_META.displays;

export default function DigitalDisplaysPage() {
  return <ScenePage variant="displays" />;
}
