import { AdminSection } from "@/components/admin/admin-section";

export const metadata = { title: "Reports · Channel Cast" };

export default function Page() {
  return <AdminSection slug="reports" />;
}
