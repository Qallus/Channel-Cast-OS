import { AudioManagement } from "@/components/audio/audio-management";

export const metadata = { title: "Audio Management · Channel Cast" };

export default function Page() {
  return <AudioManagement />;
}
