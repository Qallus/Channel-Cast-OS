import { DisplaysPage } from "@/components/displays/displays-page";

export const metadata = { title: "Digital Displays · Channel Cast" };

export default function Page() {
  return <DisplaysPage />;
}
