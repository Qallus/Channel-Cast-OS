import { AddDeviceView } from "@/components/devices/add-device-view";

export const metadata = { title: "Add a device · Channel Cast" };

export default function AddDevicePage() {
  return <AddDeviceView />;
}
