import { DevicesManager } from "@/components/devices/devices-manager";

export const metadata = { title: "Device Fleet · Channel Cast" };

export default function DevicesPage() {
  return <DevicesManager />;
}
