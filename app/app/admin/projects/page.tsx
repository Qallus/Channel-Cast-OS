import { AdminSection } from "@/components/admin/admin-section";

export const metadata = { title: "Projects · Channel Cast" };

export default function Page() {
  return <AdminSection slug="projects" />;
}
