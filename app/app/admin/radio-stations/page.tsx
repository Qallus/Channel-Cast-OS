import { AdminSection } from "@/components/admin/admin-section";

export const metadata = { title: "Radio Stations · Channel Cast" };

export default function Page() {
  return <AdminSection slug="radio-stations" />;
}
