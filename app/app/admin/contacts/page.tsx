import { AdminSection } from "@/components/admin/admin-section";

export const metadata = { title: "Contacts · Channel Cast" };

export default function Page() {
  return <AdminSection slug="contacts" />;
}
