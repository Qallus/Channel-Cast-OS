import { AdminSection } from "@/components/admin/admin-section";

export const metadata = { title: "Advertisers · Channel Cast" };

export default function Page() {
  return <AdminSection slug="advertisers" />;
}
