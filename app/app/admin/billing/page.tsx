import { AdminSection } from "@/components/admin/admin-section";

export const metadata = { title: "Billing · Channel Cast" };

export default function Page() {
  return <AdminSection slug="billing" />;
}
