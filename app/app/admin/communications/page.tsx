import { AdminSection } from "@/components/admin/admin-section";

export const metadata = { title: "Communications · Channel Cast" };

export default function Page() {
  return <AdminSection slug="communications" />;
}
